x-openmetadata-environment: &openmetadata-environment
  OPENMETADATA_CLUSTER_NAME: elt-governance
  SERVER_PORT: 8585
  SERVER_ADMIN_PORT: 8586
  LOG_LEVEL: INFO
  AUTHENTICATION_PROVIDER: basic
  AUTHENTICATION_PUBLIC_KEYS: "[http://localhost:${OPENMETADATA_PORT:-8585}/api/v1/system/config/jwks]"
  AUTHENTICATION_ENABLE_SELF_SIGNUP: "false"
  AUTHORIZER_ADMIN_PRINCIPALS: "[admin]"
  AUTHORIZER_PRINCIPAL_DOMAIN: open-metadata.org
  AUTHORIZER_ALLOWED_REGISTRATION_DOMAIN: '["open-metadata.org"]'
  AUTHORIZER_INGESTION_PRINCIPALS: "[ingestion-bot]"
  DB_DRIVER_CLASS: org.postgresql.Driver
  DB_SCHEME: postgresql
  DB_PARAMS: allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
  DB_USER: openmetadata_user
  DB_USER_PASSWORD: ${OM_DB_PASSWORD:?Defina OM_DB_PASSWORD em .env.openmetadata}
  DB_HOST: openmetadata-postgres
  DB_PORT: 5432
  OM_DATABASE: openmetadata_db
  SEARCH_TYPE: elasticsearch
  ELASTICSEARCH_HOST: openmetadata-elasticsearch
  ELASTICSEARCH_PORT: 9200
  ELASTICSEARCH_SCHEME: http
  ELASTICSEARCH_USER: ""
  ELASTICSEARCH_PASSWORD: ""
  ELASTICSEARCH_CLUSTER_ALIAS: elt-governance
  PIPELINE_SERVICE_CLIENT_ENABLED: "false"
  SECRET_MANAGER: db
  OPENMETADATA_HEAP_OPTS: -Xms1G -Xmx1G
  MASK_PASSWORDS_API: "true"

services:
  openmetadata-postgres:
    image: postgres:16-alpine
    container_name: elt-openmetadata-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: openmetadata_db
      POSTGRES_USER: openmetadata_user
      POSTGRES_PASSWORD: ${OM_DB_PASSWORD:?Defina OM_DB_PASSWORD em .env.openmetadata}
    volumes:
      - openmetadata-postgres-data:/var/lib/postgresql/data
    networks:
      - openmetadata
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openmetadata_user -d openmetadata_db"]
      interval: 10s
      timeout: 5s
      retries: 15

  openmetadata-elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.3.0
    container_name: elt-openmetadata-elasticsearch
    restart: unless-stopped
    environment:
      discovery.type: single-node
      ES_JAVA_OPTS: -Xms1g -Xmx1g
      xpack.security.enabled: "false"
    volumes:
      - openmetadata-elasticsearch-data:/usr/share/elasticsearch/data
    networks:
      - openmetadata
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:9200/_cluster/health | grep -Eq '\"status\":\"(green|yellow)\"'"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 30s

  openmetadata-migrate:
    image: docker.getcollate.io/openmetadata/server:${OPENMETADATA_VERSION:-2.0.2}
    container_name: elt-openmetadata-migrate
    restart: "no"
    command: ./bootstrap/openmetadata-ops.sh migrate
    environment: *openmetadata-environment
    networks:
      - openmetadata
    depends_on:
      openmetadata-postgres:
        condition: service_healthy
      openmetadata-elasticsearch:
        condition: service_healthy

  openmetadata-server:
    image: docker.getcollate.io/openmetadata/server:${OPENMETADATA_VERSION:-2.0.2}
    container_name: elt-openmetadata-server
    restart: unless-stopped
    environment: *openmetadata-environment
    ports:
      - "127.0.0.1:${OPENMETADATA_PORT:-8585}:8585"
    networks:
      - openmetadata
    depends_on:
      openmetadata-postgres:
        condition: service_healthy
      openmetadata-elasticsearch:
        condition: service_healthy
      openmetadata-migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8586/healthcheck"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 60s

  openmetadata-bootstrap:
    image: docker.getcollate.io/openmetadata/ingestion:${OPENMETADATA_VERSION:-2.0.2}
    profiles: ["tools"]
    environment:
      OPENMETADATA_URL: http://openmetadata-server:8585/api
      OM_ADMIN_USERNAME: ${OM_ADMIN_USERNAME:-admin@open-metadata.org}
      OM_ADMIN_PASSWORD: ${OM_ADMIN_PASSWORD:?Defina OM_ADMIN_PASSWORD em .env.openmetadata}
      ELT_POSTGRES_HOST: elt-postgres
      ELT_POSTGRES_PORT: 5432
      ELT_POSTGRES_USER: ${ELT_POSTGRES_USER:?Defina ELT_POSTGRES_USER em .env.openmetadata}
      ELT_POSTGRES_PASSWORD: ${ELT_POSTGRES_PASSWORD:?Defina ELT_POSTGRES_PASSWORD em .env.openmetadata}
      ELT_AIRFLOW_URL: http://elt-airflow-webserver:8080
      ELT_AIRFLOW_USERNAME: ${ELT_AIRFLOW_USERNAME:?Defina ELT_AIRFLOW_USERNAME em .env.openmetadata}
      ELT_AIRFLOW_PASSWORD: ${ELT_AIRFLOW_PASSWORD:?Defina ELT_AIRFLOW_PASSWORD em .env.openmetadata}
    volumes:
      - ./scripts/openmetadata/bootstrap.py:/opt/openmetadata/bootstrap.py:ro
    entrypoint: ["python", "/opt/openmetadata/bootstrap.py"]
    networks:
      - openmetadata
      - elt
    depends_on:
      openmetadata-server:
        condition: service_healthy

volumes:
  openmetadata-postgres-data:
  openmetadata-elasticsearch-data:

networks:
  openmetadata:
    name: elt-openmetadata-network
  elt:
    external: true
    name: ${ELT_DOCKER_NETWORK:?Defina ELT_DOCKER_NETWORK em .env.openmetadata}
